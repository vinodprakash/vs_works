== One Drive

==== Usage

        git clone https://github.com/vinodprakash/vs_works.git
        cd vs_works
        bundle install
        rake db:migrate
        rails s

Go to /upload/index route to upload a file to onedrive.
